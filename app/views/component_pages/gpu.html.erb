<% provide(:title, 'GPU') %>
<!DOCTYPE html>
<html>
<head>
 <title>Computer | <%= yield(:title) %></title>
</head>
<center><h1>GPU</h1></center>

<center>
  <body>
    
     <table style="width:100%">
      <tr>
        <th></th>
        <th><center>ID</center></th>
        <th><center>VRAM</center><th>
        <th><center>Price</center><th>
      </tr>

      <% @gpu.each do |f| %>
        <tr>
          <td><input type="radio" name="gpu_choice" value=f.id /></td>
          <td><%= f.id %></td>
          <td><%= f.vram %></td>
          <td>$<%= f.price %></td>
        </tr>
      <% end %>
    </table>
  </center>


    <div align="right">
    <%= link_to "Next", component_pages_hd_path,
                                  class: "btn btn-large btn-primary" %>
  </div>
  </body>
</html>
