<% provide(:title, 'RAM') %>
<!DOCTYPE html>
<html>
<head>
 <title>Computer | <%= yield(:title) %></title>
</head>
<center><h1>RAM</h1></center>

<center>
  <body>
    
     <table style="width:100%">
      <tr>
        <th></th>
        <th><center>ID</center></th>
        <th><center>Size</center></th>
      </tr>

      <% @rvm.each do |f| %>
        <tr>
          <td><input type="radio" name="ram_choice" value=f.id /></td>
          <td><%= f.id %></td>
          <td><%= f.size %> GB</td>
        </tr>
      <% end %>
    </table>
  </center>


    <div align="right">
    <%= link_to "Next", component_pages_gpu_path,
                                  class: "btn btn-large btn-primary" %>
  </div>

  </body>
</html>
