<% provide(:title, 'Select A Price') %>

<center><h1>Select Max Price</h1></center>

<div class="row center jumbotron" >
  <div class="col-md-4 col-md-offset-4">
  	<%= form_for @computer do |f| %>
  	<%= f.label :by_price, "Max Build Price" %>
  	<%= f.number_field :by_price, :value => "", :placeholder => 0, class: 'form-control' %>

	<br/>
	<br/>
	<center>
			<%= f.submit "Submit", class: "btn btn-lg btn-success" %>
			<a>&nbsp&nbsp&nbsp&nbsp&nbsp</a>
			<%= link_to build_by_price_option_path, class: "btn btn-lg btn-danger" do %> Cancel
			<% end %>
	</center>
	<% end %>
	</div>
</div>
<center>
	<%= link_to component_pages_motherboard_path, class: "btn btn-lg btn-danger" do %> Continue →
	<% end %>
</center>
